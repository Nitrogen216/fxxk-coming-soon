# DATA_AND_EVAL.md — Data and Evaluation Specifications

<!-- Generated by PAPER_TO_CODE_PROMPT.md Stage 1. Supplement based on paper content. -->
<!-- CRITICAL: The "Paper Target Metrics" YAML block is parsed by the loop agent. -->

---

## Research Context

*[What specific research question is being answered? What table/figure are we trying to reproduce?]*

---

## Dataset Specifications

| Field | Value |
|-------|-------|
| Dataset name | *[e.g., ImageNet ILSVRC 2012]* |
| Version/split | *[e.g., standard train/val split]* |
| Official source | *[URL]* |
| License | *[e.g., non-commercial research]* |
| Train samples | *[N]* |
| Val samples | *[N]* |
| Test samples | *[N]* |
| Input format | *[e.g., RGB images, 224×224]* |

### Dataset Download Commands

```bash
# Exact commands to reproduce the dataset setup
# [e.g.:]
# wget https://... -O data/raw/dataset.tar.gz
# tar -xzf data/raw/dataset.tar.gz -C data/raw/
# python data/preprocess.py --input data/raw/ --output data/processed/
```

### Preprocessing Steps

1. *[Step 1: e.g., resize to 256px on shorter side]*
2. *[Step 2: e.g., center crop to 224×224 for val; random crop for train]*
3. *[Step 3: normalize with mean=[...] std=[...]]*

**Train augmentations**: *[list all — include random horizontal flip, color jitter, etc.]*
**Val/test transforms**: *[list — usually minimal: resize + crop + normalize]*

---

## Paper Target Metrics

> **This section is machine-parsed by the loop agent. Format must be exact YAML.**
> Copy values directly from the paper's main results table.

```yaml
## Paper Target Metrics
# Source: [Paper Title], [Venue Year]
# Table reference: [e.g., "Table 2, row 'Ours (full model)'"]
primary_metrics:
  - name: accuracy             # must match function name in src/eval/metrics.py
    value: ~                   # exact number from paper (e.g., 0.847 for 84.7%)
    higher_is_better: true
    source: "Table N, row '...', column '...'"
    notes: ""                  # e.g., "average of 3 seeds", "test set only"
    eval_command: "python eval.py --metric accuracy"

  # - name: f1_score
  #   value: ~
  #   higher_is_better: true
  #   source: ""
  #   eval_command: "python eval.py --metric f1_score"

secondary_metrics:             # checked only at effort >= max
  # - name: params_millions
  #   value: ~
  #   higher_is_better: false
  #   source: ""
  #   eval_command: null       # measured during training, not via eval script

ablation_targets:              # checked only at effort = beast
  # - name: accuracy_without_component_X
  #   value: ~
  #   source: "Table N, ablation row '-X'"
  #   eval_command: "python eval.py --ablation no_component_x"
```

---

## Evaluation Framework

### Primary Metrics

*[For each metric: exact formula, implementation notes, and common pitfalls.]*

**Accuracy**:
- Formula: `correct / total`
- Implementation: `(preds.argmax(1) == targets).float().mean()`
- Pitfall: *[e.g., "Paper uses top-1 not top-5"]*

**F1 Score** *(if applicable)*:
- Formula: *[macro / micro / weighted — specify which the paper uses]*
- Implementation: `sklearn.metrics.f1_score(y_true, y_pred, average='macro')`
- Pitfall: *[e.g., "Paper does not specify averaging — default macro based on class balance"]*

### Evaluation Protocol

- **Test set**: *[exact dataset split used for final evaluation]*
- **Multiple runs**: *[does paper average over N seeds? specify]*
- **Significance tests**: *[does paper report confidence intervals or p-values?]*

### Reproducibility Requirements

```python
# Random seed must be set before dataset creation and model initialization
set_seed(42)   # or paper-specified seed

# Evaluation must be deterministic
torch.backends.cudnn.deterministic = True
model.eval()
with torch.no_grad():
    ...
```

---

## Experimental Protocols

### Hyperparameters

*[Reproduce Table N from paper — every hyperparameter with source reference.]*

| Parameter | Value | Source |
|-----------|-------|--------|
| learning_rate | ~ | Table N / Section N |
| batch_size | ~ | Section N |
| epochs | ~ | Section N |
| optimizer | ~ | Section N |
| *[Add all hyperparameters]* | | |

### Hardware & Runtime

- **GPU**: *[paper's hardware — e.g., 8× A100 80GB]*
- **Training time**: *[wall-clock — e.g., ~12 hours]*
- **Our hardware**: *[fill in when running reproduction]*

---

## Tolerance Policy

Configured in `LOOP_STATE.md`. Defaults:

| Effort | Tolerance | Coverage |
|--------|-----------|----------|
| `lite` | 15% | primary only |
| `balanced` | 10% | primary only |
| `max` | 5% | primary + secondary |
| `beast` | 2% | all including ablations |
